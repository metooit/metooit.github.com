<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: git | K.I.S.S]]></title>
  <link href="http://metooit.github.com/blog/categories/git/atom.xml" rel="self"/>
  <link href="http://metooit.github.com/"/>
  <updated>2013-02-05T20:20:39+08:00</updated>
  <id>http://metooit.github.com/</id>
  <author>
    <name><![CDATA[metooit]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[深入学习 Git]]></title>
    <link href="http://metooit.github.com/blog/2013/02/05/dive-into-git/"/>
    <updated>2013-02-05T00:46:00+08:00</updated>
    <id>http://metooit.github.com/blog/2013/02/05/dive-into-git</id>
    <content type="html"><![CDATA[
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[学习 Git]]></title>
    <link href="http://metooit.github.com/blog/2013/02/04/learning-git/"/>
    <updated>2013-02-04T12:59:00+08:00</updated>
    <id>http://metooit.github.com/blog/2013/02/04/learning-git</id>
    <content type="html"><![CDATA[<p>寒假里的日子，有点让人觉得不知到要做什么，我很怕自己浪费时间，所以觉得要学会自己找事情做，然后我觉得有必要认真的学习一下git。Google了一下，找到了这个<a href="http://learn.github.com/p/intro.html">git官方学习指南</a>，我就照着这个先看一看git吧。：-）</p>

<hr />

<h3>关于Git</h3>

<h4>首先，看一下我的git版本吧：</h4>

<p><div class='bogus-wrapper'><notextile><figure class='code'><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
</pre></td><td class='code'><pre><code class=''><span class='line'>$ git --version</span></code></pre></td></tr></table></div></figure></notextile></div>
git version 1.7.10.4
这应该是最新的版本了吧。</p>

<h4>然后，如何取得帮助呢？</h4>

<p><div class='bogus-wrapper'><notextile><figure class='code'><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
</pre></td><td class='code'><pre><code class=''><span class='line'>$ git log --help
</span><span class='line'>$ git help log</span></code></pre></td></tr></table></div></figure></notextile></div></p>

<p><img class="git" src="/pictures/git-lang.png" title="git-lang" ></p>

<h4>Git的特点：</h4>

<ul>
<li>几乎可以离线工作，并且很快（相对与SVN来说）</li>
<li>安装体积小</li>
<li>基于snapshot（快照），而不是changesets
<img class="git" src="/pictures/snapshots.png" title="snapshots" ></li>
<li>如果要更加深入的了解git，请看<a href="http://eagain.net/articles/git-for-computer-scientists/">Git for Computer Scitentists</a></li>
<li>分支的代价更小，合并方便</li>
</ul>


<hr />

<h3>Git的初始化设置</h3>

<p>第一次使用Git时，首先要学会设置好一些东西，比如名字和邮件地址。设置的命令是<code>git config</code>,如果加上<code>--global</code>,这个命令会将设置信息存储在<code>~/.gitconfig</code>。
<div class='bogus-wrapper'><notextile><figure class='code'><figcaption><span>git-config </span></figcaption>
<div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
</pre></td><td class='code'><pre><code class=''><span class='line'>$ git -config --global user.name "your name here"
</span><span class='line'>$ git -config --global user.email "your email here"</span></code></pre></td></tr></table></div></figure></notextile></div>
git也可以设定用户默认的文本编辑器，比如emacs
<div class='bogus-wrapper'><notextile><figure class='code'><figcaption><span>git-editor </span></figcaption>
<div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
</pre></td><td class='code'><pre><code class=''><span class='line'>$ git -config --global core.editor 'emacs'</span></code></pre></td></tr></table></div></figure></notextile></div>
可以运行以下命令来查看当前的设置情况：
<div class='bogus-wrapper'><notextile><figure class='code'><figcaption><span>git-check </span></figcaption>
<div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
</pre></td><td class='code'><pre><code class=''><span class='line'>git config user.name</span></code></pre></td></tr></table></div></figure></notextile></div>
或者呢，你可以自己查看，git首先会读取<code>/etc/gitconfig</code>,然后再读取<code>~/.gitconfig</code>，最后是<code>.git/config</code>。</p>

<hr />

<h3>初始化新的repo</h3>

<p>要初始化一个repo，在相应目录中使用<code>git init</code>。
<div class='bogus-wrapper'><notextile><figure class='code'><figcaption><span>repo-init </span></figcaption>
<div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
<span class='line-number'>3</span>
</pre></td><td class='code'><pre><code class=''><span class='line'>$ mkdir myproject
</span><span class='line'>$ cd myproject
</span><span class='line'>$ git init</span></code></pre></td></tr></table></div></figure></notextile></div>
现在我们建立了一个新的code repo，在这个目录中拥有自己的<code>.git</code>目录，有一些自己的设置。
然后我们可以用
<div class='bogus-wrapper'><notextile><figure class='code'><figcaption><span>add-commit </span></figcaption>
<div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
</pre></td><td class='code'><pre><code class=''><span class='line'>$ git add .
</span><span class='line'>$ git commit -m "initial commit"</span></code></pre></td></tr></table></div></figure></notextile></div>
现在我们拥有了一个新的project，其中有一个commit，如何查看这个项目的状态呢？
<div class='bogus-wrapper'><notextile><figure class='code'><figcaption><span>git-log </span></figcaption>
<div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
</pre></td><td class='code'><pre><code class=''><span class='line'>$ git log</span></code></pre></td></tr></table></div></figure></notextile></div></p>

<hr />

<h3>克隆一个git repo</h3>

<p>git可以和多个端口链接，其中最常使用的是<code>ssh</code>、<code>http</code>、<code>git protocol</code>。其格式是 <code>git clone [url]</code>, 其中的<code>url</code>的格式是<code>git://(hostname)/(path).git</code>。
<div class='bogus-wrapper'><notextile><figure class='code'><figcaption><span>git-clone </span></figcaption>
<div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
<span class='line-number'>3</span>
</pre></td><td class='code'><pre><code class=''><span class='line'>$ git clone git://github.com/schacon/mungit.git
</span><span class='line'>$ cd mungit
</span><span class='line'>$ ls</span></code></pre></td></tr></table></div></figure></notextile></div>
同样，在很多的情况下，你也可以使用http来clone：
<div class='bogus-wrapper'><notextile><figure class='code'><figcaption><span>git-clone </span></figcaption>
<div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
</pre></td><td class='code'><pre><code class=''><span class='line'>$ git clone http://github.com/schacon/mungit.git</span></code></pre></td></tr></table></div></figure></notextile></div></p>

<hr />

<h3>通常的工作流程</h3>

<p>现在该设置的都设置好了，项目也会建了，然后做什么呢？
通常的工作流程是：
* 修改文件
* 看看你改变了什么
* 阶段性的看看你希望commit的改变
* commit你的阶段性成果
* 重新来过……
最简单的工作流程是：
* 修改文件
* commit你的改变
* 重复……
最简单的example：
Edit files, <code>git commit -a</code>, repeat.</p>

<p>用到了<code>staging area</code>:
<div class='bogus-wrapper'><notextile><figure class='code'><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
</pre></td><td class='code'><pre><code class=''><span class='line'>$ git status</span></code></pre></td></tr></table></div></figure></notextile></div>
然后我们可以看到一个<code>changed but not updated</code>区域，这里的文件就是<code>unstaged</code>的文件。你必须stage一个文件，然后再commit。</p>

<p><img class="git" src="/pictures/localoperations.png" title="localoperations" ></p>

<p>Git用户使用<code>git add</code>命令来既tracking文件也stage changes。
如果在staging阶段你不仅想看到什么文件被修改了或者staged，并且比较文件可以使用<code>git diff</code>.</p>
]]></content>
  </entry>
  
</feed>
